import React from "react";
import { StyledStartButton } from "./StyledStartButton";

const StartButton = ({ onClick }) => {
  return <StyledStartButton onClick={onClick}>StartButton</StyledStartButton>;
};

export default StartButton;
